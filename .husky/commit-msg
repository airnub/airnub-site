#!/bin/sh
set -eu

SCRIPT="$(git rev-parse --show-toplevel)/scripts/validate-commit.sh"

if [ ! -x "$SCRIPT" ]; then
  echo "Commit validation script not found at $SCRIPT" >&2
  exit 127
fi

exec "$SCRIPT" "$1"
